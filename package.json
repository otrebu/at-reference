{
  "name": "at-reference",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for @at-reference tooling - parse and validate @path/to/file references",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "keywords": [
    "claude",
    "claude-code",
    "at-reference",
    "markdown",
    "validator"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.25.0"
}
